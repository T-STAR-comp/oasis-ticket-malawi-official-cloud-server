cd ticket-malawi-cloud-server

npm run build:all
(set SERVE_FRONTEND=true in .env for unified site + API)

Dev:  npm run dev
Prod: npm run start   (runs server.cjs)

cPanel / Passenger startup file: server.cjs
(requires npm run build so dist/index.js exists)
