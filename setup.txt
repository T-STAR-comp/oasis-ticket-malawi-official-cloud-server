LOCAL DEV
---------
cd ticket-malawi-cloud-server
SERVE_FRONTEND=true in .env
npm run dev

SHIP TO PRODUCTION (before git push)
------------------------------------
npm run ship
(uses 8GB Node heap for frontend build — close other apps if OOM)

This builds the main app into public/client/ and compiles dist/.
Commit and push both folders with your code changes.

DATABASE (after deploy)
-----------------------
npm run db:migrate:login
npm run db:migrate:tiers

CPANEL / SPACESHIP
------------------
git pull in oasis-ticket-malawi-official-cloud-server
npm install --omit=dev
(create .env from .env.example)
Restart Node app (startup file: server.cjs)

No build step needed on the server if you ran ship before pushing.
